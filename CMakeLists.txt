cmake_minimum_required(VERSION 3.20)
project(BdUI LANGUAGES "CXX")
include_directories(${PROJECT_SOURCE_DIR}/include)
option("Shared" "Compile to Shared lib" OFF)

set(LIBRARY_OUTPUT_PATH ./bin)
message(STATUS ${CMAKE_CXX_STANDARD})

file(GLOB_RECURSE SRC ${PROJECT_SOURCE_DIR}/src/*.cpp)

add_subdirectory("${PROJECT_SOURCE_DIR}/lib/libjpeg-turbo")
if(WIN32)
set(PLATFORM_LIB "dwmapi")
link_directories("${PROJECT_SOURCE_DIR}/lib/windows")
endif()
if(Shared)
add_library(${PROJECT_NAME} SHARED ${SRC})
else()
add_library(${PROJECT_NAME} STATIC ${SRC})
endif()
add_subdirectory(${PROJECT_SOURCE_DIR}/Test)
target_link_libraries(${PROJECT_NAME} ${PLATFORM} opengl32 FreeImage)